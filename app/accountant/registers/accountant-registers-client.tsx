'use client';

// Client selection belongs to My Clients. The legacy cross-client register
// screen is intentionally kept as an inert compatibility module so existing
// deployments can overwrite the old file when applying a changed-files ZIP.
export default function AccountantRegistersClient() {
  return null;
}
